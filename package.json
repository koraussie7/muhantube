{
  "name": "muhantube-monorepo",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "dev": "pnpm --filter @muhantube/web dev",
    "build": "pnpm --filter @muhantube/web build",
    "preview": "pnpm --filter @muhantube/web preview",
    "build:player": "pnpm --filter @muhantube/player build"
  },
  "engines": {
    "node": ">=20"
  }
}
